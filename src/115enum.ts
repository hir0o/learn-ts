export {};
// イナム
// 列挙型

enum Months {
  January = 1,
  Fabruary,
  Merch,
  April,
  May,
  Jun,
  july,
  August,
}

console.log(Months);

enum COLORS {
  RED = "#FF0000",
  WHITE = "#FFFFFF",
  GREEN = "#008000",
  BLUR = "#0000FF",
}

console.log(COLORS);

// 要素の追加
enum COLORS {
  YELLOW = "#FFFF00",
}

console.log(COLORS.YELLOW);
